[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -m uvicorn backend_sitram:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/status"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
